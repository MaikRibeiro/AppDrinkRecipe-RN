import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputContainer: {
        flexDirection: "row",
        alignItems: "center",
        gap: 8,
    },
    textInput: {
        borderWidth: 0.5,
        padding: 8,
        borderRadius: 4,
        borderColor: "gray",
        flex: 1,
    },
});